spring:
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
        enabled: true
        refresh-enabled: true
        server-addr: localhost:8848
        file-extension: yml
  application:
    name: background
  profiles:
    active: local
server:
  port: 8081