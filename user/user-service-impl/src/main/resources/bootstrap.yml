spring:
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
        enabled: false
        refresh-enabled: true
        server-addr: localhost:8848
        file-extension: yml
      discovery:
        server-addr: localhost:8848
  application:
    name: user
  profiles:
    active: local
server:
  port: 8082